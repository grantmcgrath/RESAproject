import React from "react";

const ListView = ({app}) => {
  return (
    <div>
      Testing List View
      <div>
        <p>{app.stagersName}</p>
      </div>
    </div>
  )
}

export default ListView;
